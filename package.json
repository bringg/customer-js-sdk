{
  "name": "@bringg/customer-sdk",
  "version": "0.0.1",
  "main": "BringgSDK.js",
  "scripts": {
    "test": "karma start",
    "build": "rm -rf dist && mkdir -p dist/bower_components && cp package.json dist/ && cp LICENSE dist/ && cp BringgSDK.js dist/ && cp -r bower_components/socket.io-client dist/bower_components/socket.io-client",
    "codecov": "codecov"
  },
  "devDependencies": {
    "codecov": "^2.2.0",
    "jasmine-core": "^2.5.2",
    "karma": "^1.3.0",
    "karma-chrome-launcher": "^2.0.0",
    "karma-coverage": "^1.1.1",
    "karma-jasmine": "^1.1.0",
    "karma-junit-reporter": "^1.2.0",
    "karma-mocha-reporter": "^2.2.1",
    "karma-notify-reporter": "^1.0.1",
    "karma-phantomjs-launcher": "^1.0.2"
  },
  "engines": {
    "node": ">=0.10.0"
  }
}
